# Pinned so the published R2 figures reproduce exactly rather than approximately.
# The figures are quoted to three decimals in the README and are stable across
# scikit-learn 1.3.2 to 1.9.0; these pins are the versions they were taken on.
pyyaml==6.0.2
numpy==2.3.1
pandas==2.3.1
scikit-learn==1.8.0
